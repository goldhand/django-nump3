============
Installation
============

At the command line::

    $ easy_install django-nump3

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-nump3
    $ pip install django-nump3